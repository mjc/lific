---
title: Installation
description: Install Lific from a GitHub release, crates.io, or source.
---

The Cargo package is named `lific`.

## GitHub release binaries

Release binaries are published on [GitHub Releases](https://github.com/VoidNullable/lific/releases). Each release includes `sha256sums.txt` for checksum verification.

| Platform | Release file |
| --- | --- |
| Linux x86_64 | `lific-linux-x86_64` |
| Linux aarch64 | `lific-linux-aarch64` |
| macOS x86_64 | `lific-macos-x86_64` |
| macOS aarch64 | `lific-macos-aarch64` |
| Windows x86_64 | `lific-windows-x86_64.exe` |

Download the file for your platform. Compare it with the matching entry in `sha256sums.txt`. On Unix systems, make the downloaded file executable and place it in a directory on your `PATH`.

```bash
chmod +x lific-linux-x86_64
./lific-linux-x86_64 --help
```

On Windows, put `lific-windows-x86_64.exe` somewhere on your `PATH` and rename it to `lific.exe`. [Windows](/docs/windows) covers that platform in full, including WSL 2.

## Install with Cargo

Install the crate from crates.io with Cargo.

```bash
cargo install lific
```

Confirm that the binary is available.

```bash
lific --version
```

## Install with Nix

On x86_64 Linux, aarch64 Linux, or Apple silicon macOS, install the package
directly from the repository flake.

```bash
nix profile install github:VoidNullable/lific
```

The flake also exports `overlays.default` and `nixosModules.default` for
declarative package and `services.lific` deployments.

The user service records the stable profile executable path, so a later
`nix profile upgrade` takes effect on the next service restart without
pinning the service to an old store generation.

## Build from source

Lific requires Rust 1.88 or later. SQLite is bundled with the Rust build.

For a build without the web UI, create the frontend output directory before compiling. The binary still provides the CLI, REST API, MCP server, OAuth, and backup features. Requests for the web UI report that the frontend has not been built.

```bash
git clone https://github.com/VoidNullable/lific
cd lific
mkdir -p web/dist
cargo build --release
```

For a build with the embedded web UI, install Bun and build the frontend before compiling the Rust binary.

```bash
git clone https://github.com/VoidNullable/lific
cd lific
cd web && bun install && bun run build && cd ..
cargo build --release
```

The release binary is written to `target/release/lific`.
