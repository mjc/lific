/target
*.db
*.db-wal
*.db-shm
# LIF-262: runtime attachment blobs (content-addressed sidecar dir) live next
# to the database; never commit a dev instance's uploads.
/attachments/
.env
AGENTS.md
deploy.sh
dev-backend.sh
/.dev/

# Krita working/autosave files + editor backups (keep exported PNGs, not these)
*.kra
*~
.*-autosave.kra
